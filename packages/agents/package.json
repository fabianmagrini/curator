{
  "name": "@curator/agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "VoltAgent multi-agent runtime for the Tech Radar Curator",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@curator/shared": "workspace:*"
  }
}
